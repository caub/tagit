%% Feel free to use, reuse and abuse the code in this file.

{application, tagit, [
	{description, ""},
	{vsn, "0.1.0"},
	{modules, []},
	{registered, [tagit_sup]},
	{applications, [
		kernel,
		stdlib,
		crypto,
		public_key,
		ssl,
		cowboy
	]},
	{mod, {tagit, []}},
	{env, []}
]}.
